version: '3.9'
services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
  worker:
    build: .
    command: ./start_worker.sh
    volumes:
      - .:/app
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
    depends_on:
      - redis
